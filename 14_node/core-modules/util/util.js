const util = require('util');

// util.inspect(object[, options])
// Returns a string representation of the object, which is useful for debugging.

// util.format(format[, ...args])
// Returns a formatted string using the specified format and arguments.

// util.formatWithOptions(inspectOptions, format[, ...args])
// Returns a formatted string using the specified inspect options, format, and arguments.

// util.promisify(original)
// Converts a callback-based function into a Promise-based one.

// util.callbackify(original)
// Converts a Promise-based function into a callback-based one.

// util.debuglog(section)
// Returns a function that can be used for logging debugging messages.

// util.deprecate(fn, message)
// Marks a function as deprecated and prints a warning message when it is called.

// util.types
// An object containing utility functions for checking the types of JavaScript values.

// util.TextEncoder
// A constructor for TextEncoder objects, which encode strings into ArrayBuffer objects.

// util.TextDecoder
// A constructor for TextDecoder objects, which decode ArrayBuffer objects into strings.

// util.TextEncoder.encodeInto(string, destination[, offset])
// Encodes a string into a destination Uint8Array and returns a TextEncodeResult object.

// util.TextDecoder.decodeInto(buffer, destination[, offset])
// Decodes data from a source Uint8Array into a string and returns a TextDecodeResult object.
