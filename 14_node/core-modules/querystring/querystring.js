const querystring = require("querystring");


// querystring.escape(str)
// Returns a URL-encoded version of the input string.

// querystring.unescape(str)
// Returns the original string decoded from the URL-encoded string.

// querystring.parse(str[, sep[, eq[, options]]])
// Parses a URL query string into an object.

// querystring.stringify(obj[, sep[, eq[, options]]])
// Converts an object into a URL query string.

// querystring.encodeURIComponent(str)
// Alias for querystring.escape(str).

// querystring.decodeURIComponent(str)
// Alias for querystring.unescape(str).