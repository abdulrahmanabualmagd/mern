const url = require("url");

// url.parse(urlString[, parseQueryString[, slashesDenoteHost]])
// Parses a URL string into a URL object.

// url.format(urlObject)
// Serializes a URL object into a URL string.

// url.resolve(from, to)
// Resolves a target URL relative to a base URL.

// url.URL
// The URL class provides utilities for URL resolution and parsing.

// url.URLSearchParams
// The URLSearchParams class provides utilities for working with query string parameters.
